// package Controllers

// import (
// 	"Batreyna/Models"
// 	"net/http"

// 	"github.com/gin-gonic/gin"
// )

// func FetchInventories(c *gin.Context) {
// 	var inventories []Models.Inventory
// 	if err := Models.DB.Model(Models.Inventory{}).Preload("Items").Find(&inventories).Error; err != nil {
// 		ReturnErr(c, err)
// 	}

// 	c.JSON(http.StatusOK, inventories)
// }
